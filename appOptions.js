// Options for the application
var options = {

    // Does not make tweets or store information in the firebase database
    // Only prints the tweets it would make into the console.
    PRINT_ONLY: true

}



module.exports = {
    options: options
}
